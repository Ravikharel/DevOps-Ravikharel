pipeline{
    agent any
    stages { 
        stage("Running docker compose"){ 
            steps{ 
                script { 
                    sh "docker compose up -d" 
                }
            }
        }
    }
}